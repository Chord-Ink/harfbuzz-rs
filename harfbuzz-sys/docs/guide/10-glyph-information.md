# Glyph information

*Adapted from the HarfBuzz 14.3.0 documentation, which is licensed under the terms in the HarfBuzz COPYING file.*

> **Note from the translation:** This chapter is an unwritten placeholder in the
> upstream HarfBuzz manual. The source file (`docs/usermanual-glyph-information.xml`)
> contains the chapter title, the section heading below, and a single empty
> paragraph — no prose, no code examples, and no tables. It is also not currently
> included in the manual's table of contents. The headings are reproduced here so
> the guide series stays aligned with upstream chapter numbering; the body will be
> filled in if and when upstream writes it.
>
> Until then, the material you might expect to find here is covered elsewhere:
> glyph indices, glyph positions, and the contents of the output buffer are
> discussed in the Buffers, language, script and direction guide; mapping glyphs
> back to the input text is discussed in the Clusters guide; and the font-level
> functions for querying glyph names, extents, and advances are discussed in the
> Fonts and faces guide.

## Names and numbers
